name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: bootstrap
      run: ./configure.py --bootstrap
    - name: Run a multi-line script
      run: |
        ./ninja all
        ./ninja_test
